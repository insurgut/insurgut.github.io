name: TG Deploy
on:
  push:
    paths: ['source/**']
jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Process & Send
        env:
          TOKEN: ${{ secrets.TOKEN }}
          CHAT_ID: "6222585321"
        run: |
          set +e
          
          TRIMMED_TOKEN=$(echo "$TOKEN" | tr -d '\n' | tr -d '\r')
          git config core.quotePath false

          # 1. ПОДГОТОВКА ДАННЫХ
          SHORT_SHA=$(git rev-parse --short HEAD)
          COMMIT_URL="https://github.com/$GITHUB_REPOSITORY/commit/$GITHUB_SHA"
          
          # Время МСК + Ссылка
          TIME=$(TZ=Europe/Moscow date "+%Y-%m-%d %H:%M:%S")
          TIME_URL="https://time.is/Moscow"

          if git rev-parse --verify HEAD~1 >/dev/null 2>&1; then
            RANGE="HEAD~1 HEAD"
          else
            RANGE="4b825dc642cb6eb9a060e54bf8d69288fbee4904 HEAD"
          fi

          git diff --diff-filter=ACMRT --name-only $RANGE -- source/ | while read -r FILE; do
            echo "Processing: $FILE"
            if [ ! -f "$FILE" ] || [ ! -s "$FILE" ]; then continue; fi

            # --- ГЕНЕРАЦИЯ ПОДПИСИ (СТРОГО ДВОЙНЫЕ КАВЫЧКИ) ---
            # Telegram принимает только <a href="...">, одинарные кавычки запрещены API
            
            LINK_COMMIT="<a href=\"$COMMIT_URL\">$SHORT_SHA</a>"
            LINK_TIME="<a href=\"$TIME_URL\">$TIME</a>"
            
            CAP="$LINK_COMMIT • $LINK_TIME"

            echo "Generated Caption: $CAP"

            # --- ОТПРАВКА ---
            curl -v -F chat_id="$CHAT_ID" \
                 -F document=@"$FILE" \
                 -F parse_mode="HTML" \
                 -F caption="$CAP" \
                 "https://api.telegram.org/bot$TRIMMED_TOKEN/sendDocument" > response.json 2> curl_log.txt || true
            
            # --- ПРОВЕРКА ---
            if ! grep -q '"ok":true' response.json; then
                echo "::error::Upload Failed for $FILE"
                cat response.json
                
                # Запасной вариант (Текст без ссылок, если вдруг API заглючит)
                ERR_MSG="$SHORT_SHA • $TIME (HTML Error)"
                curl -s -F chat_id="$CHAT_ID" \
                     -F document=@"$FILE" \
                     -F caption="$ERR_MSG" \
                     "https://api.telegram.org/bot$TRIMMED_TOKEN/sendDocument" || true
            else
                echo "SUCCESS: Sent $FILE"
            fi
          done
          
          exit 0
